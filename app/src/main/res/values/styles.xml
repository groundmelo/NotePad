<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 主题样式 -->
    <style name="NotePadTheme" parent="@android:style/Theme.Holo.Light">
        <item name="android:colorPrimary">@color/primary</item>
        <item name="android:colorPrimaryDark">@color/primary_dark</item>
        <item name="android:colorAccent">@color/accent</item>
        <item name="android:windowBackground">@color/background</item>
    </style>
    
    <style name="NotePadTheme.Dark" parent="@android:style/Theme.Holo">
        <item name="android:colorPrimary">@color/primary_dark_mode</item>
        <item name="android:colorPrimaryDark">@color/primary_dark_dark_mode</item>
        <item name="android:colorAccent">@color/accent_dark_mode</item>
        <item name="android:windowBackground">@color/background_dark_mode</item>
        <item name="android:textColorPrimary">@color/text_primary_dark</item>
        <item name="android:textColorSecondary">@color/text_secondary_dark</item>
    </style>
    
    <!-- 笔记卡片样式 -->
    <style name="NoteCard">
        <item name="android:background">@color/card_background</item>
        <item name="android:elevation">4dp</item>
    </style>
    
    <!-- 按钮样式 -->
    <style name="RoundedButton" parent="@android:style/Widget.Button">
        <item name="android:background">@drawable/rounded_button</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:paddingVertical">8dp</item>
        <item name="android:paddingHorizontal">16dp</item>
        <item name="android:textSize">14sp</item>
    </style>
    
    <!-- 输入框样式 -->
    <style name="RoundedEditText" parent="@android:style/Widget.EditText">
        <item name="android:background">@drawable/rounded_edittext</item>
        <item name="android:paddingVertical">12dp</item>
        <item name="android:paddingHorizontal">16dp</item>
        <item name="android:textSize">16sp</item>
    </style>
</resources>